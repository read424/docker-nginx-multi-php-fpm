#upstream php-fpm{
#    server php-fpm:9000;
#}
#upstream php-fpm8{
#    server php-fpm8:9000;
#}
server {
    listen 80;
    index index.php index.html;
    server_name localhost;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    access_log /var/log/nginx/map_values.log;

    root /usr/share/nginx/html;

    #location / {
        # Utiliza el valor generado por el map en el log
    #    add_header X-Custom-Value $custom_value;
    #    return 200 "Custom value: $custom_value";
    #}

    #location ~ \.php$ {
    #    resolver 127.0.0.11 ipv6=off;
    #    try_files $uri =404;
    #    fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #    #fastcgi_pass php-fpm8:9900;
    #    #fastcgi_pass php-fpm8;
    #    fastcgi_pass $phpfpm:9000;
    #    fastcgi_index index.php;
    #    include fastcgi_params;
    #    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #    fastcgi_param PATH_INFO $fastcgi_path_info;
    #}
    include /etc/nginx/php.conf;
}